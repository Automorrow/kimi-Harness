Orchestrate a multi-phase task across team members.

By default, the task goes through four phases: research, plan, implement, and review. Each phase is assigned to members with the corresponding role (explorer, leader, worker, reviewer).

Parameters:
- team_name (string, required): The name of the team to orchestrate the task with.
- task (string, required): The task description to orchestrate across phases.
