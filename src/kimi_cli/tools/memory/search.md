Search existing persistent memory entries.

Returns all memories matching the query string (matched against titles and content).
Use this to recall previously saved project context, user preferences, or decisions.

If no query is provided, all memories are listed.
