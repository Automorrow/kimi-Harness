Save a persistent memory entry that survives across sessions.

Use this to remember important project context, user preferences, architectural decisions,
or any information that should persist between conversations.

The memory will be automatically injected into future conversations as context,
so the AI can maintain continuity without needing to re-learn.

**Guidelines:**
- Use concise, specific titles (e.g. "Project uses pnpm" not "Note about package manager")
- Keep content focused and actionable
- One memory per topic — don't cram unrelated info into one entry
- Update existing memories by saving a new entry with the same topic
